<Window x:Class="CyberSecurityChatBot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cyber Security ChatBot" Height="450" Width="700">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer VerticalScrollBarVisibility="Auto" Name="ChatScrollViewer">
            <TextBlock x:Name="ChatDisplay" TextWrapping="Wrap" FontSize="14" />
        </ScrollViewer>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,10,0,0">
            <TextBox x:Name="UserInput" Width="550" Height="30" FontSize="14" Margin="0,0,10,0"/>
            <Button Content="Send" Width="100" Height="30" Click="Button_Click"/>
        </StackPanel>
    </Grid>
</Window>
